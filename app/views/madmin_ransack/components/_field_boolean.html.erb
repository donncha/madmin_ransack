<% field_key = model.ransackable_scopes.include?(field) ? field : "#{field}_eq" %>
<% values = [[t('madmin_ransack.filters.no'), false], [t('madmin_ransack.filters.yes'), true]] %>

<%= form.label(label) %>
<%= form.select(field_key, values, include_blank: true) %>
